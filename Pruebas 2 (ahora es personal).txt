si ves esto, BORRALO. JEJE (ENRI estuvo aqui)

el id = DNI?
id autogenerado, io aumentado?
fecha de alta, de creacion?
enum quien los hace. como se hacen? (problema conla clase usuario por el enum del rol)
controlar errores, y excepciones.

el UML esta mal marcadas la cantidad de relaciones (de contrato a cleinte es solo 1 por contrato = con proveedor)
en el UML en la entidad de "CONTRATO" se repite el atributo del enum "estado de contrato"